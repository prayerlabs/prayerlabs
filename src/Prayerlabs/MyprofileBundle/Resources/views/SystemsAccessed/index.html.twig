{% extends '::base.html.twig' %}

{% block body -%}
    <h1>SystemsAccessed list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Last_login</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('prayerlabs_systems_accessed_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.lastlogin %}{{ entity.lastlogin|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('prayerlabs_systems_accessed_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('prayerlabs_systems_accessed_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('prayerlabs_systems_accessed_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
